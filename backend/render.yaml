services:
  - type: web
    name: sentinelhome-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn --chdir backend app.main:app --bind 0.0.0.0:$PORT
