"""Database boundary reserved for a later persistence phase.

The beginner version uses the in-memory store in ``store.py`` so the web flow
can be understood before MongoDB and Redis are introduced.
"""
